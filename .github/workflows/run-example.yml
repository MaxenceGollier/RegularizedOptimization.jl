name: run-example
on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  example:
    name: Run example.jl (${{ matrix.version }} - ${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        version: ['1', 'pre']
        os: [ubuntu-latest, macOS-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}

      - uses: actions/cache@v4
        env:
          cache-name: cache-artifacts
        with:
          path: ~/.julia/artifacts
          key: ${{ runner.os }}-example-${{ env.cache-name }}-${{ hashFiles('**/Project.toml') }}
          restore-keys: |
            ${{ runner.os }}-example-${{ env.cache-name }}-
            ${{ runner.os }}-example-
            ${{ runner.os }}-


      - name: Instantiate example environment
        run: |
          julia --project=paper/examples -e "using Pkg; Pkg.instantiate()"

      - name: Run example.jl
        run: |
          julia --project=paper/examples 'ENV["MLDATASETS_ACCEPT_ALL"]="true"; include("paper/examples/example.jl")'
  